package GureumLevel;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;
import java.util.Set;

public class Laundry {
    public static void main(String[] args) {
       /* Scanner scan = new Scanner(System.in);
        int color = scan.nextInt();
        int cloth = scan.nextInt();
        int time = 0;
        Map<Integer, String> arrMap = new HashMap<Integer, String>();
        String[] colors = scan.nextLine().split(" ");
        for(int i = 0; i < cloth; i++) {
            arrMap.put(scan.nextInt(), scan.next());
        }
        for(int i = 0; i < cloth; i++) {
            for(int j = 1; j < cloth; j++)  {
                if(arrMap.containsValue(colors[i]) == arrMap.containsValue(colors[j])) {
                    int a = getKey(arrMap, colors[i]);
                }
            }
        }*/
    }
}
